<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- In-app updates: lets the app hand a downloaded release APK to the
         system installer. The user still confirms every install, and Android
         rejects any APK whose signing cert differs from the installed one. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".TidyLinkApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TidyLink">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.TidyLink"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Invisible trampoline: captures shared URLs from other apps
             (Chrome, YouTube, etc.) and finishes without opening the UI. -->
        <activity
            android:name=".ShareReceiverActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <!-- text/*, NOT text/plain. The manifest filter is what decides
                     whether this app appears in a share sheet at all, so a
                     text/plain filter makes TidyLink invisible in any app that
                     shares a link under another text/* subtype - and
                     ShareReceiverActivity's own text/* check never gets to run.
                     That mismatch is why some apps' share sheets listed
                     TidyLink and others didn't. Non-URL text is handled: the
                     receiver shows "no link found" and saves nothing. -->
                <data android:mimeType="text/*" />
            </intent-filter>
        </activity>

        <!-- Exposes the downloaded update APK (app cache) to the system
             installer. Nothing else is shared: file_paths only covers
             cache/updates/. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
