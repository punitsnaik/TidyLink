## What & why

<!-- Short description of the change and the problem it solves. -->

## Checklist

- [ ] `./gradlew lint testDebugUnitTest assembleDebug` passes locally
- [ ] Unit tests added/updated for pure logic changes
- [ ] Room schema change? → version bumped, migration added, schema JSON committed
- [ ] Dependency or R8 rule change? → release build smoke-tested on a device
- [ ] User-visible strings live in `strings.xml`
