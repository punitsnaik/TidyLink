<div align="center">

# TidyLink

**A tidy home for every link you save.**

A native Android app that captures URLs shared from any app, scrapes their metadata, and uses AI to automatically categorize, tag, and summarize them - turning a messy pile of saved links into a searchable, organized library.

[![CI](https://github.com/punitsnaik/TidyLink/actions/workflows/ci.yml/badge.svg)](https://github.com/punitsnaik/TidyLink/actions/workflows/ci.yml)
[![Release](https://img.shields.io/github/v/release/punitsnaik/TidyLink?sort=semver)](https://github.com/punitsnaik/TidyLink/releases)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Android 10+](https://img.shields.io/badge/Android-10%2B-3DDC84?logo=android&logoColor=white)](#prerequisites)
[![Kotlin](https://img.shields.io/badge/Kotlin-100%25-7F52FF?logo=kotlin&logoColor=white)](#tech-stack)

<br>

[![Download the APK](https://img.shields.io/badge/Download-APK-3DDC84?style=for-the-badge&logo=android&logoColor=white)](https://github.com/punitsnaik/TidyLink/releases/latest)
&nbsp;
[![Sponsor this project](https://img.shields.io/badge/Sponsor-%E2%99%A5-db61a2?style=for-the-badge&logo=githubsponsors&logoColor=white)](https://github.com/sponsors/punitsnaik)

<sub>Free, MIT, no backend, no ads, no tracking. Sponsoring is optional and changes nothing about the app.</sub>

</div>

## What this is

You share links all day - a YouTube video from a friend, an article from X, a Reel you'll "watch later." They pile up in chat threads and browser tabs and you never find them again.

TidyLink is the place they go. Share a URL from any app and it lands in TidyLink, gets scraped for its title, description, and thumbnail, then gets a category, tags, and a one-line summary from an LLM - all in the background, all searchable.

**Everything stays on your device.** There is no backend and no analytics. AI categorization is optional and uses **your own** API key against any OpenAI-compatible endpoint - Gemini's free tier, xAI's Grok, or anything else that speaks the chat-completions API.

```
Share / Add / Import
        │
        ▼
URL canonicalization ──► duplicate? ──► reuse existing link
        │
        ▼
Placeholder saved immediately (survives crashes; WorkManager safety net)
        │
        ▼
Metadata scrape (jsoup)
  Open Graph / Twitter tags, with a social-crawler
  user-agent fallback for login-walled sites
        │
        ▼
AI classification (any OpenAI-compatible API, user-supplied keys)
  category + tags + summary; batched for bulk imports;
  automatic provider failover and retry with backoff
        │
        ▼
Room database (FTS4 + Paging 3) ──► Compose UI
```

## Features

- **Share to save** - share a URL from any app (YouTube, Instagram, X, a browser…) straight to TidyLink. The link is committed durably before the share sheet even closes, then scraped and classified in the background.
- **AI categorization** - every link gets a category, 3–5 tags, and a one-sentence summary, generated by an LLM using API keys you add in-app. Add several providers and TidyLink rotates to the next when one hits its rate limit. Keys are stored **encrypted** (Android Keystore) and never leave the device.
- **Bulk import from TXT** - import a text file containing any number of URLs. New links appear instantly as placeholders; a background worker then scrapes and classifies them - the import survives leaving the app, Doze, and even a process kill.
- **Smart duplicate detection** - URLs are canonicalized before saving: tracking parameters (`si`, `utm_*`, `fbclid`, `igsh`, `gclid`…), fragments, and www/scheme variants are normalized away, so the same YouTube short shared twice with different `?si=` tokens is recognized as one link. Existing duplicates are merged automatically on refresh.
- **Full-text search** - instant search across titles, descriptions, categories, tags, and summaries, backed by SQLite FTS4 with prefix matching ("kotl compo" finds "Kotlin Compose").
- **Built to scale** - the list is paged (Paging 3) with all filtering and sorting done in SQLite, so a 10,000-link library scrolls like a 100-link one.
- **Category filter chips** - one tap to filter by any category; an overflow sheet keeps large taxonomies manageable. "Tidy up categories" merges near-duplicates offline and, when needed, asks the LLM for a semantic merge.
- **Rich link cards** - adaptive grid (one column on phones, more on tablets) with thumbnails, category badges, summaries, and tags; tap a card for a full detail sheet with in-place refresh.
- **Video-aware** - YouTube, Shorts, Reels, TikTok, and Vimeo links get a play action; links open in the native app when installed, otherwise in a Chrome Custom Tab.
- **Multi-select & undo** - long-press to select multiple links, bulk delete or re-categorize, undo from the snackbar. Swipe a card right to refresh it, left to delete.
- **Pin & sort** - pin favorites to the top; sort by date, title, or category.
- **Backup & restore** - export the whole library as JSON and import it back on any device.
- **Offline-resilient** - links saved while offline or rate-limited are stored immediately and enriched automatically by WorkManager when the network returns.
- **First-run walkthrough** - a short intro on first launch covers sharing, search, and the optional AI setup, with a step to add a key on the spot. Skippable, and replayable any time from Settings → About → "Show intro again".

## Prerequisites

| | |
|---|---|
| **To use the app** | An Android 10 (API 29) device or newer. Optionally, an API key from any OpenAI-compatible LLM provider. |
| **To build it** | The latest stable **Android Studio** - the project uses AGP 9 and `compileSdk 36.1`, and older IDE versions won't sync it. JDK 21. |

No API keys, secrets, or configuration files are needed to build and run.

## Quick start

### 1. Install

Grab the latest APK from the [Releases page](https://github.com/punitsnaik/TidyLink/releases). The repository itself contains no binaries.

Or build it yourself:

```bash
git clone https://github.com/punitsnaik/TidyLink.git
cd TidyLink
./gradlew installDebug
```

### 2. Save your first link

On first launch a short walkthrough covers how links get in, how to find them again, and the optional AI setup. Skip it any time; replay it later from **Settings → About → "Show intro again"**.

Then: open any app, hit **Share → TidyLink**. That's it - no configuration needed to start saving.

### 3. Turn on AI categorization (optional)

Open **Settings → AI providers** in the app and add an API key. Presets are built in for the common free tiers:

| Provider | Notes |
|---|---|
| **Google Gemini** | Free, no card required - [aistudio.google.com](https://aistudio.google.com) |
| **xAI Grok** | Fast; pay-as-you-go with promotional credits - [console.x.ai](https://console.x.ai) |
| **Custom** | Any other OpenAI-compatible endpoint |

Press **Test** to verify a key before adding it. Add more than one provider and TidyLink falls back to the next when one is rate-limited - **the list order is the fallback order**. Keys are stored encrypted, only on the device.

Without a provider configured, nothing is ever sent anywhere and links simply stay "Uncategorized".

## Privacy

TidyLink has no backend and no analytics. Four things leave your device, all in service of features you can see:

1. **Link metadata → your AI provider.** The URL, page title, and description of a saved link are sent to the LLM endpoint *you* configured, for categorization. No provider configured → nothing is sent.
2. **Link domains → Google.** Cards without a thumbnail show a favicon fetched from `google.com/s2/favicons`.
3. **YouTube URLs → YouTube oEmbed** for reliable titles and thumbnails.
4. **An update check → GitHub.** At most once a week (or when you tap *Check for updates* in Settings), the app asks `api.github.com` for the latest release - an unauthenticated GET that sends nothing about you or your links. Downloading an update fetches the APK from the same GitHub release.

API keys are encrypted at rest with a hardware-backed Android Keystore key and are excluded from cloud backups and device transfers.

**No crash reporting.** This is a deliberate choice, not an oversight: TidyLink ships no
Crashlytics, Sentry, or ACRA, so a crash is never reported anywhere - not even to me.
The cost is real and worth stating plainly: if the app crashes for you, I only find out
if you [open an issue](https://github.com/punitsnaik/TidyLink/issues). That trade is
made on purpose - "no backend, no analytics" would otherwise be true only until the
first crash.

## Tech stack

| Layer | Choice |
|---|---|
| UI | Jetpack Compose, Material 3, Paging 3, Coil 3 |
| State | ViewModel + Kotlin Flows, unidirectional `UiState` |
| Storage | Room + FTS4 full-text search, paged queries |
| Scraping | jsoup with dual user-agent strategy |
| AI | Retrofit + kotlinx-serialization against any OpenAI-compatible endpoint |
| Background | WorkManager (durable saves, bulk enrichment sweep, classification retry) |
| Security | Android Keystore (AES/GCM) for API keys at rest |
| Language | 100% Kotlin, coroutines throughout |

Dependencies are version-catalogued in [`gradle/libs.versions.toml`](gradle/libs.versions.toml) and kept current by Dependabot.

## Project structure

```
app/src/main/java/dev/punit/tidylink/
├── MainActivity.kt                   # Compose entry point; intro-or-dashboard gate
├── ShareReceiverActivity.kt          # Invisible share-sheet trampoline
├── TidyLinkApplication.kt            # Manual DI container
├── data/
│   ├── UrlCanonicalizer.kt           # Pure URL cleaning/dedupe (unit-tested)
│   ├── local/                        # Room: entity, DAO, FTS, paging query builder
│   ├── scraper/LinkScraperService.kt # jsoup metadata scraping
│   ├── ai/AiCategorizationService.kt # LLM classification (single + batch)
│   ├── repository/LinkRepository.kt  # Pipeline, dedupe, bulk import
│   ├── settings/                     # Encrypted LLM provider store; intro-seen flag
│   └── work/                         # WorkManager workers (save, sweep, retry)
└── ui/
    ├── LinkViewModel.kt              # UiState + paged links + all user actions
    ├── dashboard/                    # Grid, search, chips, sheets, dialogs
    ├── onboarding/                   # First-run intro pager (skippable, replayable)
    └── theme/                        # Material 3 theme (dynamic color)

app/schemas/                          # Exported Room schemas, for migration tests
.github/workflows/                    # ci.yml (every push/PR), release.yml (v* tags)
```

## Design decisions worth knowing

A few things in here were not obvious and cost real debugging time. They're written down so the next person - or the next you - doesn't rediscover them the hard way.

- **The save is committed before the share sheet closes.** `ShareReceiverActivity` writes a placeholder row synchronously, then hands enrichment to WorkManager. The naive version - scrape first, then save - loses links whenever Android kills the trampoline activity, which it does often and without warning.

- **Canonicalization is a pure function, and that's the point.** `UrlCanonicalizer` has no Android dependencies, so the dedupe rules that decide whether two URLs are "the same link" are covered by fast JVM unit tests rather than an emulator. Tracking-param stripping is the kind of logic that quietly regresses.

- **Search is FTS4, not FTS5.** Android's bundled SQLite doesn't ship FTS5, so the query builder emits bare `MATCH` tokens with `*` prefixes and no FTS5-only syntax. Columns shared between the FTS table and the base table must be table-qualified, or the join silently returns an empty list instead of erroring.

- **R8 keep rules only count if R8 reads them.** An earlier version of this project kept them under `src/main/keepRules/` - not an AGP source-set convention. R8 never loaded them and stripped every kotlinx-serialization `$$serializer`, breaking AI classification and JSON import/export **in release builds only, at runtime**. Rules now live in `app/proguard-rules.pro`, wired via `proguardFiles`. Smoke-test a release build after touching dependencies.

- **Migration tests are instrumented, not Robolectric.** `MIGRATION_3_4` uses `ALTER TABLE … DROP COLUMN`, which needs SQLite 3.35+; Robolectric bundles 3.32.2. So the migration suite runs on a device via `connectedDebugAndroidTest`, and CI compiles it on every push so it can't rot unnoticed. SQLite 3.35 arrives with API 34, so that suite must be run on an API 34+ device - and a **future migration must not use `DROP COLUMN`**, because minSdk is 29.

- **Supporting Android 10 costs more than lowering `minSdk`.** Three things silently degrade below the old floor of 34, and each is handled explicitly rather than assumed:
  - **Dynamic color is API 31+.** `dynamicLightColorScheme` reads `android.R.color.system_*`, which doesn't exist on Android 10/11 - calling it unguarded throws at runtime. `TidyLinkTheme` checks `SDK_INT >= S` and falls back to the Ocean/Coral palettes.
  - **`dataExtractionRules` is API 31+.** Android 10/11 read `fullBackupContent` instead, so the API-key backup exclusion is stated twice - `res/xml/backup_rules.xml` mirrors `res/xml/data_extraction_rules.xml`. They are not alternatives. Change one, change the other, or Android 10 users silently get their encrypted keys backed up.
  - **`FLAG_ACTIVITY_REQUIRE_NON_BROWSER` is API 30+.** It's an int constant, so it inlines happily at minSdk 29 and is then *ignored* by Android 10: `startActivity` succeeds into a browser, never throws, and the Custom Tab fallback becomes unreachable. `openLink` version-splits and queries the package manager for non-browser handlers on 29.

- **`androidTest` method names are snake_case, not backticked.** Spaces in a DEX `SimpleName` need DEX 040, emitted only at minSdk 30+. At 29 the dexer emits DEX 039 and D8 fails the `assembleDebugAndroidTest` build outright. Unit tests under `src/test/` are JVM-only, never dexed, and keep their backticks.

- **The intro flag is read synchronously, on purpose.** `OnboardingStore` uses SharedPreferences, not DataStore, because `MainActivity` branches on it during the first composition. Prefs load in the store's constructor, so the first frame already knows the answer. A DataStore flow would emit its default first, flashing the empty dashboard before the intro replaced it - and "modernizing" it to DataStore reintroduces exactly that.

- **There is deliberately no OkHttp logging interceptor.** It would log `Authorization` headers - the user's API keys - if anyone ever wired it in. The dependency is absent so it can't be.

- **A missing keystore falls back to the debug key, loudly.** `assembleRelease` without `keystore.properties` still produces an installable APK for local testing, but prints a boxed warning. Debug-signed APKs carry the universally known `android/android` key - anyone can forge an update for one, and real installs can never be upgraded by the true key afterwards. CI treats this as a hard failure (see below).

## Testing

```bash
./gradlew testDebugUnitTest          # pure JVM tests - fast, no device
./gradlew connectedDebugAndroidTest  # Room migration tests - needs a device/emulator
./gradlew lint                       # Android lint
```

Unit tests cover the logic that is pure and load-bearing: URL canonicalization, the FTS query sanitizer, the paging query builder, LLM response parsing, and category-name normalization. The instrumented suite migrates a v1, v2, and v3 database up to v4 and asserts that rows, the FTS index, and search all survive.

CI runs lint, unit tests, a debug build, and a compile of the instrumented tests on every push and PR.

## Building a release

Release builds are signed with a keystore that must live **outside** version control.

**Locally:**

1. Create a keystore and keep it somewhere safe outside the repo.
2. Create `keystore.properties` next to `settings.gradle.kts` (git-ignored):

   ```properties
   storeFile=/absolute/path/to/release.jks
   storePassword=…
   keyAlias=…
   keyPassword=…
   ```

   Prefer an absolute path pointing outside the repo, so the key can never be swept up by an accidental `git add -f`.

3. `./gradlew assembleRelease`

   A caveat that has cost real time here: this is a Java `.properties` file, and it is read literally - but the *shell you write it with* is not. `echo "storePassword=abc\$1234" > keystore.properties` lets zsh expand `$1234` to nothing, silently storing a truncated password. The build then fails with the thoroughly misleading `keystore password was incorrect`. Use a quoted heredoc (`<<'EOF'`), an editor, or `read -s` - and prefer a password with no `$` in it at all.

**Via CI (the real path):** pushing a `v*` tag triggers [`release.yml`](.github/workflows/release.yml), which builds, signs, verifies, and attaches the APK to a GitHub Release. The tag drives the naming - `v1.0` publishes a Release titled **TidyLink-1.0** with **`TidyLink-1.0.apk`** attached - so there is nothing to rename by hand. It needs these repository secrets:

| Secret | Value |
|---|---|
| `RELEASE_KEYSTORE_BASE64` | `base64 -i release.jks` - re-upload whenever the keystore file changes, **including a password rotation**: `keytool -storepasswd` rewrites the `.jks` bytes, so the password and this secret must always be updated together |
| `RELEASE_STORE_PASSWORD` | |
| `RELEASE_KEY_ALIAS` | |
| `RELEASE_KEY_PASSWORD` | |

…plus one repository **variable**:

| Variable | Value |
|---|---|
| `RELEASE_CERT_SHA256` | From `keytool -list -v -keystore release.jks \| grep "SHA256:"` |

The workflow refuses to publish a debug-signed APK, and - if `RELEASE_CERT_SHA256` is set - refuses to publish one signed with a *stale* key. A `CN=` check can't catch that case, because a rotated key usually keeps the same subject name; only the fingerprint distinguishes them.

`versionCode` in `app/build.gradle.kts` must increase on **every** release, even a re-tag of the same `versionName` - it's what Android compares to decide whether something is an upgrade.

## Contributing

Bug reports and PRs welcome - see [CONTRIBUTING.md](CONTRIBUTING.md). Security issues: see [SECURITY.md](SECURITY.md).

## Support

TidyLink has no backend, which also means it has no revenue and nothing to upsell you. No paid tier, no ads, no data resold. The only running cost is your own LLM provider's, billed by them, to you.

If it earns a place on your home screen, you can [sponsor the project](https://github.com/sponsors/punitsnaik) - or just star the repo and file a good bug report. Both help.

## License

[MIT](LICENSE)
